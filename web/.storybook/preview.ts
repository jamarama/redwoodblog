import { RouterDecorator } from "@redwoodjs/testing/web";

export const decorators = [RouterDecorator];

export const parameters = {
  actions: { argTypesRegex: "^on[A-Z].*" },
  controls: {
    matchers: {
      color: /(background|color)$/i,
      date: /Date$/,
    },
  },
};
